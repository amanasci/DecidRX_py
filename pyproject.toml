[project]
name = "decidrx"
version = "0.1.0"
description = "CLI decision engine to prioritize tasks"
requires-python = ">=3.10"
dependencies = ["rich"]

[project.scripts]
decidrx = "decidrx.cli:main"

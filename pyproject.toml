[project]
name = "decidrx"
version = "0.1.1"
description = "CLI decision engine to prioritize tasks"
requires-python = ">=3.10"
dependencies = ["rich"]

[project.scripts]
decidrx = "decidrx.cli:main"
